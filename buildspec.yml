version: 0.2
phases:
  install:
    runtime-versions:
      docker: 18
  pre_build:
    commands:
      - npm install
      - npm install -g serverless
  build:
    commands:
      - serverless deploy -v --stage ${STAGE} | tee deploy.out
